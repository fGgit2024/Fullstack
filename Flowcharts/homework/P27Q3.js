console.log(
  "======================================עמוד 27 תרגיל 3====================================="
);
var num = +prompt("Please input a number");
var EvenNumber = 0;

while (EvenNumber <= num) {
  console.log(EvenNumber), (EvenNumber = EvenNumber + 2);
}
